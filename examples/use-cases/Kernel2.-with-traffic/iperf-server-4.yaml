---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: iperf-server-4
  labels:
    app: iperf-server-4
spec:
  replicas: 4
  selector:
    matchLabels:
      app: iperf-server-4
  template:
    metadata:
      labels:
        app: iperf-server-4
      annotations:
        networkservicemesh.io: kernel://kernel-mech-red-trafficv4/nsm-1
    spec:
      containers:
        - name: iperf
          image: networkstatic/iperf3:latest
          imagePullPolicy: IfNotPresent
          command: ["tail", "-f", "/dev/null"]
